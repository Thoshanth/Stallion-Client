services:
  - type: web
    name: stallion-fitness-backend
    runtime: node
    root: backend
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
